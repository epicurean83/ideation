# 신사업 발굴 · 기획 하네스

이 저장소는 신사업 아이디어를 **발굴 → 검증 → POC → 기획서 → 심의**까지 끌고 가는 작업 공간이다.
`.claude/skills/`에 설치된 23개 스킬을 아래 프로세스 순서대로 조합해 쓴다.
스킬은 도구이고, 이 문서가 운영 규칙이다. 충돌하면 이 문서가 우선한다.

## 사업 모델 정의 (합격선)

이 저장소가 찾는 것은 "돈 되는 사업"이 아니다.
**많은 사람이 쓰고, 스스로 유지비를 벌고, 한 사람이 운영하는 서비스**다.

아래 네 조건이 모든 게이트·스코어카드·산출물의 기준선이다. 스킬 기본값(금액 TAM, LTV/CAC, 수익성 추정, 투자자 피치)과 충돌하면 이 절이 이긴다.

| # | 조건 | 합격선 | 이 조건으로 죽는 아이디어 |
|---|---|---|---|
| 1 | **규모** | 도달 가능 활성 사용자(RAU) 천장 **100만 명 이상** | 모수 자체가 100만 미만인 틈새 |
| 2 | **지속** | 이익은 필요 없다. **유지비 ≤ 확보 가능 수입** | 사용자가 늘수록 적자가 커지는 원가 구조 |
| 3 | **운영** | **1인 자율 운영.** 사용자가 10배가 돼도 사람 손은 그대로 | 심사·상담·검수·중개에 사람이 붙는 구조 |
| 4 | **개발** | **POC까지 1인 개발(12주).** 확장은 투자 유치 후 | 인허가·하드웨어 양산·신규 연구가 선행되는 구조 |

조건 2를 오해하지 말 것: "수익성 없어도 된다"는 **돈 이야기를 안 해도 된다는 뜻이 아니다.** 오히려 반대다.
이익 목표가 없으므로 **원가가 곧 생사**다. 매출 추정보다 원가 추정이 먼저이고 더 정밀해야 한다.

### 핵심 지표

| 지표 | 정의 | 기본 합격선 |
|---|---|---|
| **RAU** (Reachable Active Users) | 3년 내 도달 가능한 월간 활성 사용자 천장. **금액이 아니라 사람 수** | ≥ 1,000,000 |
| **CPU_월** (Cost per Active User) | 활성 사용자 1인당 월 원가. 인프라 + API/모델 호출 + 스토리지 + 트래픽 + 외부 서비스 | 녹색 ≤ 30원 / 황색 30~100원 / 적색 > 100원 |
| **RPU_월** (Revenue per Active User) | 활성 사용자 1인당 월 수입. 후원·스폰서·광고·그랜트·소수 유료 전환 합산 | 가정마다 비교 사례 1건 필수 |
| **유지 배수** (Sustain Ratio) | RPU_월 ÷ CPU_월 | ≥ 1.0, 12개월 내 도달 경로가 숫자로 |
| **운영 부하** (Ops Load) | 활성 1만 명당 **주당 사람 개입 시간** | ≤ 0.15시간 (= 100만 명에서 주 15시간) |
| **POC 범위** | 1인 개발 기간 | ≤ 12주, 기성 부품만 |

RPU 상한 참고: **1인당 연 2,000~4,500원은 대형 미디어의 천장값**이다. 무료 대중 서비스는 그 1/3~1/10(연 200~1,500원, 월 17~125원)에서 가정을 시작한다. 100만 명 × 월 30원 = 월 3,000만 원이 이 모델의 현실적인 유지비 상한선이라고 보면 된다.

### 즉시 탈락 규칙 (Kill Rules)

어느 단계에서든 아래가 확인되면 게이트 판정을 기다리지 않고 종료·피벗한다. `PIPELINE.md`에 사유를 적는다.

1. **선형 인건비.** 사용자 수에 비례해 사람 손이 드는 구조. "초기에는 수동으로"는 허용하되, 100만 시점의 자동화 경로가 문서에 없으면 탈락.
2. **원가 폭발.** 100만 명 시점 월 유지비가 확보 가능 수입의 2배를 넘는 구조. 특히 **사용자 요청마다 생성형 모델을 호출하는 설계**는 기본적으로 적색이다. 캐시·배치·소형 모델·온디바이스로 CPU를 먼저 깎고 다시 재본다.
3. **모수 부족.** 도달 모수 자체가 100만 미만인 세그먼트.
4. **선행 조건.** POC 전에 인허가, 오프라인 물류, 하드웨어 양산, 미검증 신기술 연구가 필요한 구조.
5. **무료 대체재.** 정부·공공·재단·플랫폼이 이미 같은 것을 무료로 제공하고 있는 영역.

### 유지비 확보 수단 (우선순위)

위에서부터 시도한다. 아래로 갈수록 1인 운영 부하가 커진다.

1. **원가를 0에 가깝게 설계** — 가장 싼 수입은 쓰지 않은 돈이다. 캐시, 정적 생성, 클라이언트 연산, 무료 티어 한계 안에서의 설계.
2. **클라우드 크레딧·공익 그랜트** — 스타트업 프로그램, 오픈소스·공익 크레딧. 기한이 있으므로 **소진일을 문서에 적는다.**
3. **소수 유료·후원** — 전환 1~3% 가정. 결제·환불·문의가 전부 셀프서비스여야 조건 3을 안 깬다.
4. **스폰서·제휴 1~2건** — 건수가 적어야 1인이 관리한다. 10곳과 계약하는 순간 영업이 직업이 된다.
5. **광고** — 최후. 1인당 단가가 낮고 운영·정책 부하가 크다.

### 자율 운영 체크리스트 (조건 3)

S4에서 `04-product/ops-autonomy.md`로 남긴다. 하나라도 "사람이 한다"면 100만 명 시점의 대안을 같은 줄에 적는다.

- 가입·인증·결제·환불·탈퇴: 전부 셀프서비스
- 콘텐츠·신고·어뷰징: 자동 판정 우선, 사람은 예외 큐만. 예외 큐의 유입률을 추정해 적는다
- 장애: 자동 복구 + 알림. 수동 배포·수동 롤백 없음
- 문의: FAQ·봇 1차 처리. 사람 개입 주당 상한을 숫자로
- 데이터·백업·비용: 임계치 알림과 자동 차단(원가 폭발 방지 회로)

### POC와 확장 (조건 4)

- **POC 정의:** 1인이 12주 안에, 기성 부품으로, 핵심 가정 1개를 실사용 데이터로 검증하는 최소 구현. 예쁨·확장성·완결성은 범위 밖이다.
- POC 산출물에는 반드시 **실측 CPU**와 **실측 운영 부하**가 들어간다. 추정치를 실측으로 교체하는 것이 POC의 절반이다.
- **확장 계획:** 투자를 유치하면 무엇이 풀리는지 명시한다. "사람을 뽑아 운영한다"는 조건 3 위반이므로 확장 사유가 될 수 없다. 돈으로 풀리는 것은 보통 원가 선결제(인프라 약정), 도달 가속(마케팅), 인허가·제휴 같은 시간 단축이다.

## 스킬 기본값 오버라이드 (사업 모델)

설치된 스킬은 대부분 "이익을 내는 스타트업"을 전제로 쓰여 있다. 아래 항목은 스킬 본문을 무시하고 이 표대로 만든다.

| 스킬 | 스킬 기본값 | 이 프로젝트 규칙 |
|---|---|---|
| `market-sizing` | TAM/SAM/SOM을 **금액**으로 | **사람 수**로 계산한다. TAM = 전체 모수, SAM = 언어·기기·플랫폼으로 접근 가능한 수, SOM = 3년 내 활성(RAU). 금액 환산은 부록으로만 남기고, 스코어카드 7번(확장 여지·시장 가치) 채점 근거로만 쓴다. 같은 파일에 **SOM × CPU_월 = 월 유지비**를 반드시 병기한다 |
| `startup-design` Phase 7 (Financial) | 수익 추정, 3개 시나리오, 손익분기 | `05-financial/`에 **원가 모델**을 쓴다. `unit-cost.md`(CPU 구성요소별 단가와 사용자 규모별 곡선: 1만 / 10만 / 100만), `sustain-model.md`(RPU 수단별 가정 + 비교 사례 + 유지 배수 도달 경로). 3년 손익 추정은 만들지 않는다 |
| `startup-design` Phase 8 (Scorecard) | 7차원: 문제 강도 / 시장 규모 / 경쟁 우위 / 실현성 / 사업 모델 명료성 / 창업자 적합도 / 타이밍 | 아래 "새 스코어카드 7차원"으로 교체 |
| `startup-design` `references/industry-benchmarks.md` | SaaS·마켓플레이스 LTV/CAC/ACV/churn | 쓰지 않는다. 벤치마크는 **무료 대중 서비스**에서 가져온다: MAU/DAU 비율, D7·W4 리텐션, 1인당 인프라 원가, 1인당 후원·광고 단가 |
| `startup-design` Phase 3.5 (Research Gate) | 시장 규모·경쟁 강도 기준 신호등 | 신호등 기준을 조건 1·2로 바꾼다. 도달 천장과 CPU가 판정 축이다 |
| `lean-startup` | 혁신 회계 = 매출·전환·LTV | 혁신 회계 지표 = **활성 사용자 증가율, D7·W4 리텐션, CPU_월, 운영 부하**. 매출은 유지 배수의 분자로만 등장한다 |
| `prioritize` (RICE) | Reach = 영향 사용자 수, Effort = 인·월 | Reach = **100만 도달 경로 기여도**, Effort = **1인 기준 주(週)**. 팀 규모 가정 금지 |
| `startup-pitch` | 투자자용. 시장 규모·수익성·성장률 | 두 벌을 만든다. (a) 심의용: 도달·유지비·1인 운영. (b) 투자 유치용: 도달 실적 + 투자로 무엇이 풀리는가. 어느 쪽도 이익 곡선을 주장하지 않는다 |
| `startup-positioning`, `obviously-awesome` | 경쟁 대안 = 유료 경쟁 제품 | 경쟁 대안에 **무료 대체재·공공 서비스·"아무것도 안 하기"**를 반드시 포함한다 |
| `blue-ocean-strategy` ERRC | 가치·원가 트레이드오프 깨기 | Eliminate·Reduce는 **CPU와 운영 부하를 깎는 축**으로 먼저 쓴다. 조건 2·3이 곧 ERRC의 원가 측이다 |
| `crucible`, `critique` | 일반 반박 | 반박 대상에 네 조건을 명시적으로 포함시킨다. 특히 "정말 100만이 쓰나", "이 원가로 버티나", "혼자 운영되나" 세 질문은 빠뜨리지 않는다 |
| `prd` | 사업성·수익 모델 섹션 | 수익 모델 섹션 대신 **유지비 계획**(CPU·RPU·유지 배수·크레딧 소진일)과 **운영 자동화 계획**을 넣는다 |

### 새 스코어카드 7차원

`06-validation/scorecard.md`는 아래 7차원을 1~10점으로 채운다. 판정 기준은 기존과 같다(8~10 진행, 6~7 조건부, 4~5 우려, 1~3 중단).

| 차원 | 무엇을 보나 | 관문 |
|---|---|---|
| 1. 문제 강도 | 사람들이 이미 우회책을 쓰고 있나. 안 풀려도 그만인가 | |
| 2. 도달 천장 | RAU 100만 경로가 산술적으로 그려지나. 모수 → 접근 가능 → 활성 전환이 각각 근거가 있나 | **관문** |
| 3. 원가 구조 | CPU_월이 녹색인가. 사용자 증가 시 원가가 선형 이하인가 | **관문** |
| 4. 유지비 확보 | RPU 수단이 2개 이상이고 각각 비교 사례가 있나. 유지 배수 1.0 경로가 숫자로 있나 | |
| 5. 자율 운영성 | 운영 부하가 사용자 수와 무관한가. 예외 큐가 한 사람의 주당 상한 안에 들어오나 | **관문** |
| 6. 1인 POC 실현성 | 12주 안에 기성 부품으로 도나. 선행 인허가·하드웨어·연구가 없나 | **관문** |
| 7. 확장 여지 · 시장 가치 | 투자가 들어오면 무엇이 풀리나. 돈으로 안 풀리는 병목이 남아 있나. **금액 기준 시장이 투자를 받을 만한 크기인가**(조건 4의 확장 단계에서만 의미를 갖는다. 금액 TAM은 여기서만 채점한다) | |

**관문 차원 규칙.** 2·3·5·6은 네 조건에 직결된 탈락 축이다. **이 중 하나라도 4점 미만이면 평균과 무관하게 판정은 「중단 또는 피벗」이다.** 다른 차원의 높은 점수로 상쇄할 수 없다. 나머지 1·4·7은 평균에 들어간다.

이 규칙이 있어야 상방 축을 스코어카드에 넣어도 안전하다. 시장 규모는 이 모델에서 탈락 축이 아니라 상방 축이므로 독립 차원으로 세우지 않고 7번 안에서 채점한다. 독립 차원으로 두면 "시장은 크니까"로 도달·원가 결함이 평균에 묻힌다.

**구 차원 → 새 차원 매핑.** 스킬 기본값을 쓰던 문서를 옮길 때 참고한다. 빠진 것이 아니라 옮겨간 것이다.

| 스킬 기본 차원 | 어디로 갔나 |
|---|---|
| 문제 강도 | 1번 그대로 |
| 시장 규모 | 두 갈래로 나뉜다. 사람 수 규모 → **2번 도달 천장**(관문), 금액 규모 → **7번 확장 여지·시장 가치**(상방) |
| 사업 모델 명료성 | **3번 + 4번**. 이 모델에서 "사업 모델이 명료하다" = "유지비가 얼마고 무엇으로 덮는지가 숫자로 분명하다"이므로, 측정 가능한 두 축으로 쪼갰다 |
| 실현성 | **6번 1인 POC 실현성**. 팀 기준이 아니라 1인 12주 기준으로 좁혔다 |
| 경쟁 우위 | 차원에서 빠지고 **G2 통과 조건 4번**(무료 대체재와 다른 축이 특정됐나)으로 옮겼다. 무료 서비스끼리는 점유율이 아니라 대체 가능성이 문제라 점수보다 통과/미통과가 맞다 |
| 창업자 적합도 | **5번 + 6번에 흡수**. 1인이 만들고 1인이 운영하는 모델에서는 창업자 적합도가 곧 자율 운영성과 POC 실현성으로 나타난다 |
| 타이밍 | 차원에서 빠지고 **S0 프리플라이트와 G2 신뢰도 판정**으로 옮겼다 |
| — | **5번 자율 운영성** 신설 (조건 3) |

## 디렉터리 규약

```
ideas/
  PIPELINE.md                 # 전체 아이디어 목록과 현재 단계 (포트폴리오 보드)
  <slug>/                     # kebab-case, 예: ai-care-robot
    PROGRESS.md               # startup-design이 만드는 체크포인트. 세션 재개 기준점
    00-intake/                # 브리프, 프리플라이트, 브레인스톰, 인터뷰 원문
    01-discovery/             # 도달 모수·대체재·고객 리서치 (raw/ 포함)
    02-strategy/              # 린 캔버스, 포지셔닝, ERRC 그리드
    03-brand/                 # Full 모드에서만
    04-product/               # POC 범위·개발 로그·자율 운영 설계 (S3부터 필수)
    05-financial/             # 원가 모델(unit-cost.md), 유지비 모델(sustain-model.md)
    06-validation/            # POC 계측, 스코어카드, 실험 설계
    discovery/                # product-discovery 산출물 (brief, interview-guide, synthesis, one-pager)
    research/<topic-slug>/    # deepdive 실행 1건당 폴더 1개 (plan.md, sources/, claims.csv, memo.md, 보고서)
    papers/                   # paper-lookup 검색 로그와 초록·전문 (query-log.md, <id>.md)
    crucible/                 # crucible 토론 전문 (YYYY-MM-DD-<slug>.md)
    docs/                     # 기획서(PRD/RFC), 피치 스크립트, 심의 자료, references.bib
```

스킬별 경로 오버라이드:

- `startup-design`, `startup-competitors`, `startup-positioning`, `startup-pitch`의 `{project-name}/`은 항상 `ideas/<slug>/`다. 저장소 루트에 프로젝트 디렉터리를 만들지 않는다.
- `product-discovery`, `interview-snapshot`의 `discovery/{name}/`은 `ideas/<slug>/discovery/`다.
- `crucible` 전문은 `~/Documents/...`가 아니라 `ideas/<slug>/crucible/`에 쓴다. 중복 판정용 glob도 그 경로를 본다.
- `market-sizing`, `synthesize-interviews`가 참조하는 `context/*.md`는 없다. 대신 `ideas/<slug>/00-intake/brief.md`와 `01-discovery/*.md`를 읽는다.
- `lenny-podcast`는 트랜스크립트 아카이브가 없으므로 framework-only 모드로 답한다. 인용을 만들어내지 않는다.
- `deepdive`의 research 폴더는 `ideas/<slug>/research/`다. 스킬이 `research/`·`docs/research/`·`~/deepdive/`를 후보로 고르지만 이 프로젝트에서는 항상 이 경로다. 교차 실행 위키(`~/.claude/research/wiki/`)와 applications ledger는 홈 디렉터리에 두는 스킬 기본값을 따른다.
- `paper-lookup` 결과는 `ideas/<slug>/papers/`에 남긴다. 검색마다 `query-log.md`에 데이터베이스, 엔드포인트, 파라미터, 조회일, 건수를 적어 재현 가능하게 한다.
- `citation-management`의 BibTeX는 `ideas/<slug>/docs/references.bib` 하나로 모은다.

## 운영 원칙

- **출력 언어는 한국어.** 스킬 본문이 영어여도 산출물·대화는 한국어로 쓴다. 고유명사, 프레임워크 이름, 코드 식별자는 원문 유지.
- **증거 없는 주장은 쓰지 않는다.** 모든 수치에 출처·날짜·신뢰도(High/Medium/Low)를 붙인다. 웹 검색이 막히면 `[Knowledge-Based — 검증 필요]` 표시를 달고 신뢰도를 한 단계 낮춘다.
- **원가 추정은 계산식을 남긴다.** CPU_월은 "월 30원"이 아니라 "요청 1건당 X원 × 월 평균 Y건 + 스토리지 Z원 = 30원"으로 쓴다. 근거 없는 단가는 `[Assumption]`으로 표시하고 실측 계획을 같은 줄에 적는다.
- **게이트는 정직하게.** 각 단계 끝의 Go/No-Go는 사용자가 듣고 싶은 답이 아니라 근거가 지지하는 답을 낸다. 스킬의 Radical Honesty Protocol을 그대로 적용한다.
- **한 아이디어 = 한 디렉터리.** 산출물은 전부 `ideas/<slug>/` 아래에 쌓는다. 채팅에만 남기고 끝내지 않는다.
- **비싼 스킬은 명시 호출만.** `crucible` Decision 모드, `startup-design` Full 모드, 딥 리서치 웨이브는 사용자가 지시했을 때만 돌린다.
- **superpowers 플러그인은 이 프로젝트에서 비활성.** 브레인스토밍은 `startup-design` Phase 2와 `blue-ocean-strategy`가 대신한다.

## 리서치 스킬 라우팅

조사 요청이 들어오면 먼저 **어떤 질문인가**로 스킬을 고른다. 세 스킬은 역할이 겹치지 않는다.

| 질문 유형 | 스킬 | 예 |
|---|---|---|
| 도달 모수·대체재·업계 구조를 **넓게** 훑는다 | `startup-design` Wave 1·2, `startup-competitors` | "이 문제를 가진 사람이 몇 명이고 지금 뭘 쓰고 있나?" |
| **하나의 결정**에 답하기 위한 데스크 리서치 | `deepdive` | "이 원가를 1/10로 깎는 방법이 있나", "X 기술은 실제로 어떻게 동작하나" |
| 논문·학술 근거·기술 타당성 | `paper-lookup` | "이 방식의 정확도를 검증한 연구가 있나", "이 DOI 전문 가져와" |
| 인용 정리·검증·서지 | `citation-management` | "PRD 참고문헌을 BibTeX로", "이 인용 정보가 맞나" |

**deepdive 사용 규칙**

- 명시 호출만: `/deepdive <질문>`. description이 러시아어라 자연어 트리거는 기대하지 않는다. 대화·산출물은 한국어로 쓴다.
- 질문이 네 조건 중 **어느 것을 푸는지** 한 줄로 적고 시작한다. 어디에도 안 걸리면 그 리서치는 안 해도 되는 리서치다.
- 깊이는 게이트의 무게에 맞춘다. **shallow**(5~7 소스, 15분): S0·S1의 탐색 질문, "X가 뭔가". **medium**(12~18 소스, 1시간): S2·S4의 가설 검증, 게이트 근거. **deep**(25~35 소스, 3시간): G4 판정이나 S5 심의 직전처럼 틀리면 비싼 결정. deep은 Plan-review gate에서 사용자의 명시적 "OK"를 기다린다.
- deepdive는 Decision Spec에 **if-then 포크**가 하나 이상 있어야 medium 이상으로 돈다. 포크가 안 나오면 이 질문은 결정과 무관하다는 뜻이니 shallow로 낮추거나 `issue-tree`로 질문부터 다시 세운다.
- 같은 질문을 `startup-design` 리서치 웨이브와 `deepdive` 양쪽에 돌리지 않는다. 웨이브가 지형을 그리고, deepdive는 그 지형 위의 특정 갈림길에 답한다.
- Phase 6.9 보고서 내보내기(HTML/PDF/DOCX)는 pandoc·mmdc가 없어 실패한다. 마크다운 보고서와 `memo.md`가 산출물이다. `finish.py`가 6.9 때문에만 빨간불이면 그 사실을 적고 진행한다. 다른 phase 빨간불은 스킬 규칙대로 되돌아가 채운다.
- deepdive `memo.md`의 권고와 포크 결과는 해당 게이트 판정 문서(`reach-cost-gate.md`, `scorecard.md`)에 `[research/<topic-slug>]`로 인용한다.

**paper-lookup 사용 규칙**

- 기술 기반 아이디어에서 "이게 되긴 하나"를 물을 때 쓴다. 벤더 자료나 블로그가 아니라 peer-reviewed 근거가 필요한 주장이 대상이다.
- 결과에는 어떤 DB에 어떤 질의를 던졌는지 provenance를 남긴다. 한 DB가 비어 있으면 "없다"가 아니라 "여기엔 색인 안 됨"이라고 쓴다.
- 1,000건·50콜을 넘길 검색은 사용자에게 먼저 묻는다.
- API 키는 전부 선택이다. 없으면 낮은 rate limit으로 진행하고, 어떤 키가 도움이 되는지 한 줄만 알린다. `.env`가 있어도 지정된 4개 변수 외에는 읽지 않는다.

**citation-management 사용 규칙**

- S5에서 `docs/prd.md`와 `docs/one-pager.md`에 인용된 논문·보고서를 `references.bib`로 모으고 `validate_citations.py`로 검증한 뒤 심의에 올린다.
- Google Scholar 스크립트는 `scholarly` 미설치로 쓰지 않는다. OpenAlex와 PubMed로 대체한다.

## 프로세스: 6단계 + 5게이트

```
S0 문제 정의 ─G0─▶ S1 기회 탐색 ─G1─▶ S2 도달·원가 검증 ─G2─▶ S3 POC·수요 검증 ─G3─▶ S4 지속·운영 설계 ─G4─▶ S5 기획서·심의
```

원칙은 **싸고 빠른 검증을 먼저, 비싼 리서치는 나중에**다. 앞 단계에서 죽일 수 있는 아이디어를 뒤 단계까지 끌고 가지 않는다.
이 모델에서 가장 싼 검증은 **산수**다. 모수 × 전환율로 100만이 안 나오거나, 원가 계산식이 적색이면 리서치 이전에 끝난다.
각 단계는 아래 표의 스킬을 순서대로 쓴다. 표에 없는 스킬을 끼워 넣어도 되지만, 표에 있는 필수 스킬은 건너뛰지 않는다.

### S0. 문제 정의 (반나절)

목적: "무엇을 풀려는가"를 MECE하게 적고, 즉시 탈락 사유가 없는지 본다.

| 순서 | 스킬 | 용도 | 산출물 |
|---|---|---|---|
| 1 | `issue-tree` (Why/What) | 막연한 기회 영역을 원인·구성요소 트리로 분해 | `00-intake/issue-tree.md` |
| 2 | `startup-design` Phase 0.5만 | 지배적 기존 해법, 선례 실패, 규제 즉사 요인 3건 검색. **무료 대체재(공공·재단·플랫폼)를 별도로 확인** | `00-intake/preflight.md` |
| 3 | 모수 냅킨 계산 | 이 문제를 가진 사람이 몇 명인지 한 줄 산수. 출처 1건 | `00-intake/preflight.md` 안에 |
| 4 | `deepdive` shallow (선택) | 도메인이 낯설 때 "X는 어떻게 돌아가나" 한 번. 트리에서 모르는 가지가 있을 때만 | `research/<topic>/memo.md` |
| 5 | `crucible --council` | 5인 페르소나 30초 돌 던지기 | 채팅 (파일 불필요) |

**G0 통과 조건:** 한 문장 문제 정의 + 대상 고객 가설 + 프리플라이트에 빨간불 없음 + **모수가 100만 이상일 여지가 있음**(정밀할 필요 없다. 자릿수만 맞으면 된다). 빨간불이면 `PIPELINE.md`에 사유를 적고 종료.

### S1. 기회 탐색 (1일)

목적: 첫 아이디어에 성급히 수렴하지 않고 변형을 넓게 본 뒤, 네 조건을 통과할 가능성이 가장 큰 하나를 고른다.

| 순서 | 스킬 | 용도 | 산출물 |
|---|---|---|---|
| 1 | `startup-design` Phase 1~2 (Fast Track) | 인테이크 인터뷰 + 변형 3~8개 발산·수렴 | `00-intake/brief.md`, `brainstorm.md`, `PROGRESS.md` |
| 2 | `blue-ocean-strategy` Six Paths + ERRC | 대체 산업·비고객·보완재에서 미개척 공간 탐색. Eliminate·Reduce는 원가·운영 부하 축으로 먼저 본다 | `02-strategy/errc-grid.md` |
| 3 | `jobs-to-be-done` | 고객이 "고용"하는 일(기능·감정·사회적)로 문제 재정의 | `01-discovery/jtbd.md` |
| 4 | **4조건 사전 필터** | 변형마다 조건 1~4를 ○/△/× 로 표시. ×가 하나라도 있으면 그 변형은 후보에서 뺀다 | `00-intake/variant-ranking.md` 안에 |
| 5 | `prioritize` (Value/Effort 또는 ICE, Effort는 1인 주 단위) | 살아남은 변형 스택랭킹 | `00-intake/variant-ranking.md` |

**G1 통과 조건:** 검증 대상 변형 1개 확정, JTBD 한 문장, 핵심 가정 5개 이내로 목록화, **4조건 필터에 ×가 없음.** 여기서 `PIPELINE.md`에 아이디어를 등록한다.

### S2. 도달 · 원가 검증 (2~3일)

목적: 100만 명에 닿는 경로가 있는지, 그 규모에서 유지비를 감당할 수 있는지를 **숫자로** 확인한다. 이 단계의 주인공은 시장 금액이 아니라 **사람 수와 원가**다.

| 순서 | 스킬 | 용도 | 산출물 |
|---|---|---|---|
| 1 | `startup-design` Phase 2.5~3 (Wave 1·2) | 리서치 깊이 결정 후 모수·대체재 병렬 리서치 | `01-discovery/reach-analysis.md`, `alternatives-landscape.md`, `industry-trends.md`, `confidence-dashboard.md` |
| 2 | `market-sizing` (사람 수 모드) | TAM/SAM/SOM을 **사람 수**로 top-down·bottom-up 양쪽 계산. SOM = RAU. 마지막 줄에 SOM × CPU_월 = 월 유지비 | `01-discovery/reach-sizing.md` |
| 3 | **원가 모델 초안** | CPU_월 계산식을 부품별로. 1만 / 10만 / 100만 명 구간의 월 원가 곡선. 적색이면 설계를 바꿔 다시 계산 | `05-financial/unit-cost.md` |
| 4 | `startup-competitors` | 상위 대안 3~5곳. **무료 대체재와 공공 서비스를 반드시 포함.** 그들의 원가 구조와 수입원도 본다 | `01-discovery/competitors-report.md`, `funding-landscape.md` |
| 5 | `market-research` | 리뷰 사이트·뉴스 기반 보완 조사 (필요 시) | `01-discovery/market-research.md` |
| 6 | `deepdive` medium | 웨이브 후 남은 **핵심 가정 1~2개**만 결정 포크로 검증. 예: "이 API 단가가 2년 내 내려가나", "이 채널로 100만에 닿은 선례가 있나" | `research/<topic>/` |
| 7 | `paper-lookup` (기술 아이디어만) | 핵심 기술의 성능·한계를 peer-reviewed 근거로 확인 | `papers/query-log.md`, `01-discovery/tech-feasibility.md` |
| 8 | `startup-design` Phase 3.5a~3.5 | 검증 에이전트 실행 후 게이트. 신호등 기준은 도달 천장과 CPU | `01-discovery/verification-report.md`, `reach-cost-gate.md` |

**G2 통과 조건:**

1. RAU 천장 ≥ 100만이고, 모수 → 접근 가능 → 활성 전환의 각 단계에 근거가 붙어 있음
2. CPU_월이 녹색 또는 황색이고, 황색이면 절감 계획이 문서에 있음
3. 유지비를 덮을 RPU 수단 후보가 2개 이상, 각각 비교 사례 1건 이상
4. 같은 것을 무료로 제공하는 대체재가 없거나, 있다면 그것과 다른 축이 특정됨
5. 신뢰도 대시보드에서 핵심 주장의 절반 이상이 Medium 이상

미달이면 피벗(S1로) 또는 종료.

### S3. POC · 수요 검증 (4~12주)

목적: 데스크 리서치를 실사용 데이터로 교체한다. **추정 CPU를 실측 CPU로, 추정 운영 부하를 실측으로 바꾸는 것이 이 단계의 핵심이다.**
인터뷰는 POC **뒤에** 한다. 쓸 물건이 없는 상태의 인터뷰는 의견을 모으지만, 쓴 사람의 인터뷰는 행동을 확인한다.

| 순서 | 스킬 | 용도 | 산출물 |
|---|---|---|---|
| 1 | `startup-design` Phase 6 (축소판) | POC 범위 정의. 12주 안에 드는 것만. 뺀 것도 적는다 | `04-product/poc-scope.md` |
| 2 | `prioritize` (RICE, Effort = 1인 주) | POC 스코프 컷. 핵심 가정 1개를 검증하는 최소 집합까지 자른다 | `04-product/poc-ranking.md` |
| 3 | **1인 개발 (≤12주)** | 실제 구현. 주 단위로 진행 상황과 막힌 지점 기록. 체크포인트에서 세션 중단, `PROGRESS.md`에 대기 상태 기록 | `04-product/poc-log.md` |
| 4 | **실사용 계측** | 활성 사용자, D7·W4 리텐션, **실측 CPU_월**, **실측 운영 부하**(주당 사람 개입 시간과 그 내역) | `06-validation/poc-metrics.md` |
| 5 | `mom-test` + `product-discovery` (Mode 2) | POC를 써 본 사람 대상 인터뷰 가이드. 과거 행동·구체적 사례 중심, 유도 질문 없이 | `discovery/interview-guide.md` |
| 6 | 사람이 인터뷰 진행 (최소 5건) | 이탈자 2건 이상 포함. 왜 안 쓰게 됐는지가 왜 쓰는지보다 정보량이 많다 | `00-intake/interviews/interview-N.md` |
| 7 | `interview-snapshot` → `synthesize-interviews` + `product-discovery` (Mode 3) | 스냅샷 1건당 1개 → 테마·페인포인트·기회 트리로 종합 | `discovery/interviews/`, `discovery/synthesis.md` |
| 8 | `startup-design` Phase 3.7 (변형) | 게이트 판정. 의견이 아니라 계측값과 행동 신호로 | `PROGRESS.md` 갱신 |

**G3 통과 조건:**

1. POC가 12주 안에 1인으로 돌아갔음. 초과했다면 초과 사유와 남은 범위가 문서에 있음
2. 실사용 유지 신호: W4 리텐션이 같은 범주 서비스의 하한 이상 (근거와 함께 기준선을 적는다)
3. 실측 CPU_월이 S2 추정의 2배 이내. 넘으면 설계를 고치고 재계측
4. 실측 운영 부하를 100만 명으로 외삽했을 때 주 15시간 이하, 또는 그렇게 만들 자동화 항목이 특정됨
5. 인터뷰에서 반증된 핵심 가정이 있으면 브리프 갱신 완료

확인율이 낮으면 세그먼트를 바꿔 S3를 반복하거나 종료.

### S4. 지속 · 자율 운영 설계 (2~3일)

목적: 100만 명 시점에 이 서비스가 **혼자 굴러가는 그림**을 그리고, 유지비가 메워지는 경로를 숫자로 만든다.

| 순서 | 스킬 | 용도 | 산출물 |
|---|---|---|---|
| 1 | `startup-positioning` | 대안 맵(무료 대체재 포함), 카테고리 선택, 포지셔닝 문장 | `02-strategy/positioning.md` |
| 2 | `obviously-awesome` | 포지셔닝 캔버스로 재정리 (심의·투자 설득 자료) | `02-strategy/positioning-canvas.md` |
| 3 | `blue-ocean-strategy` Strategy Canvas | 가치 곡선. 낮춘 축이 곧 원가·운영 부하 절감인지 확인 | `02-strategy/strategy-canvas.md` |
| 4 | `startup-design` Phase 4 + 7(오버라이드) | 린 캔버스 + **원가·유지비 모델** | `02-strategy/lean-canvas.md`, `05-financial/unit-cost.md`, `05-financial/sustain-model.md` |
| 5 | **자율 운영 설계** | 위 체크리스트 전 항목. 사람이 하는 일마다 100만 시점 대안을 병기 | `04-product/ops-autonomy.md` |
| 6 | `lean-startup` | 가정 지도 → 가장 위험한 가정부터 실험 설계. 혁신 회계 지표는 활성·리텐션·CPU·운영 부하 | `06-validation/assumptions.md`, `experiments.md` |
| 7 | `prioritize` (RICE 또는 WSJF, 1인 주 단위) | 실험·다음 개발 범위 스택랭킹 | `06-validation/experiment-ranking.md` |
| 8 | **확장 계획** | 투자 유치 시 무엇이 풀리는가. 돈으로 안 풀리는 병목도 적는다 | `02-strategy/scale-plan.md` |
| 9 | `deepdive` deep (조건부) | 스코어카드가 6~7점 경계이거나 수입원 선택지가 둘로 갈릴 때. 사용자 OK 후 실행 | `research/<topic>/memo.md` |
| 10 | `startup-design` Phase 8 (새 7차원) | 스코어카드 + 판정 | `06-validation/scorecard.md` |

**G4 통과 조건:**

1. 새 7차원 스코어카드 6점 이상이고, **관문 차원(2 도달 천장 · 3 원가 구조 · 5 자율 운영성 · 6 1인 POC 실현성)이 전부 4점 이상**
2. **유지 배수 1.0 도달 경로가 숫자로 하나 이상** (RPU 수단, 전환율 가정, 도달 시점). 크레딧에 기대는 경로면 소진일과 그 이후 계획이 함께 있음
3. 자율 운영 설계에 "사람이 한다"가 남아 있지 않거나, 남았다면 100만 시점 대안이 각각 적혀 있음
4. 포지셔닝 문장 1개
5. 3개월 안에 돌릴 수 있는 실험 3개와 각 실험의 성공·실패 기준이 숫자로

4~5점이면 조건부(우려 사항 해소 계획 첨부), 3점 이하면 종료.

### S5. 기획서 · 심의 (2~3일)

목적: 의사결정자가 읽고 결정할 수 있는 문서를 만들고, 심의 전에 스스로 가장 강하게 반박한다.

| 순서 | 스킬 | 용도 | 산출물 |
|---|---|---|---|
| 1 | `prd` | 깊이 선택: one-pager(경영진) → brief(사업부) → full PRD/RFC(실행팀). 수익 모델 섹션 자리에 **유지비 계획 + 운영 자동화 계획** | `docs/one-pager.md`, `docs/prd.md` |
| 2 | `product-discovery` (Mode 4) | POC 실사용 증거 중심 이해관계자 원페이저 | `discovery/one-pager.md` |
| 3 | `critique` | 논리 공백, 근거 없는 가정, 빠진 관점 압박 테스트. 네 조건을 반박 축에 포함 | `docs/critique-round-N.md` |
| 4 | `crucible` (Decision 모드) | 9~11인 페르소나 토론 후 판정. 사용자 명시 호출 | `crucible/YYYY-MM-DD-<slug>.md` |
| 5 | `citation-management` | 인용된 논문·보고서를 `references.bib`로 모으고 DOI·메타데이터 검증 | `docs/references.bib`, `docs/citation-report.json` |
| 6 | `startup-pitch` (2벌) | (a) 심의용 10분/5분/2분 — 도달·유지비·1인 운영. (b) 투자 유치용 — 도달 실적과 투자로 풀리는 것. 예상 Q&A, 채점 루브릭 | `docs/pitch-10min.md`, `docs/pitch-investor.md`, `docs/pitch-qa.md` |
| 7 | `critique` 2회차 | 피치 스크립트 대상으로 반복 | `docs/critique-round-N.md` |

**최종 산출물 세트:** `docs/one-pager.md` + `docs/prd.md` + `05-financial/unit-cost.md` + `05-financial/sustain-model.md` + `04-product/ops-autonomy.md` + `06-validation/poc-metrics.md` + `06-validation/scorecard.md` + `docs/pitch-10min.md` + `crucible/` 판정문 + `docs/references.bib`. 이 열 개가 갖춰져야 심의에 올린다.

## 빠른 경로 (Fast Track)

"빠른 검증", "일주일 안에", "일단 감만"이라고 하면 아래로 압축한다.

1. S0 전체
2. S1: `startup-design` Fast Track (변형 3개) + `jobs-to-be-done` 한 문장 + 4조건 필터
3. S2: Wave 1·2만. `market-sizing` 생략하고 startup-design의 모수 추정 사용. **단, CPU_월 계산식과 100만 명 구간 월 유지비는 생략하지 않는다.** 이게 이 모델의 생사 판정이다
4. S3: POC를 4주짜리 프로토타입으로 줄인다. 실측 CPU·운영 부하는 표본이 작아도 잰다. 인터뷰 3건 필수
5. S4: 린 캔버스 + 유지비 모델 + 자율 운영 체크리스트 + 스코어카드 + 실험 3개
6. S5: `prd` one-pager + `critique` 1회

Fast Track으로 통과한 아이디어는 `PROGRESS.md`에 Fast Track임을 남기고, 본 심의 전에 Full로 확장한다.

## 수시 호출 스킬

단계와 무관하게 언제든 쓴다.

- `issue-tree` How 트리: 실행 계획을 짤 때, 여러 액션 중 순서를 정할 때
- `crucible --council`: 새 아이디어가 떠오를 때 30초 sanity check. 파일 안 남김
- `crucible --solo <persona>`: 한 관점만 필요할 때 (Contrarian, First Principles, Operator 등)
- `critique`: 어떤 문서든 남에게 보내기 전
- `lenny-podcast`: PM 실무 관행 질문. framework-only 모드
- `prioritize`: 후보가 3개 이상 쌓이면 즉시
- `/deepdive` shallow: 대화 중 "X가 뭐지", "요즘 X 어떻게 됐지"가 나오면. 15분, 파일은 `research/`에 남김
- `paper-lookup`: 누군가 "연구에 따르면"이라고 말했는데 출처가 없을 때. DOI·PMID 하나면 바로 조회
- `citation-management`: 문서에 인용이 5개 넘게 쌓이면 그때부터 `references.bib`로 관리 시작
- **원가 재계산**: 설계가 바뀔 때마다. 특히 모델 호출·저장·트래픽이 늘어나는 변경은 `05-financial/unit-cost.md`를 같이 고친다

## 토큰 운영

이 저장소의 작업은 웹 검색 결과와 긴 산출물이 컨텍스트에 쌓이는 구조라, 아래를 지킨다.
비용은 검색 건수가 아니라 **턴 수 × 컨텍스트**다. 매 API 턴이 그 시점의 전체 컨텍스트를
`cache_read`로 다시 청구하므로, 길게 도는 에이전트 하나가 하루치를 태운다.

- **검색 원문을 대화에 남기지 않는다.** 리서치 웨이브는 서브에이전트에 맡기고 결론만 회수한다. 소스 본문은 `01-discovery/raw/`나 `research/<topic>/sources/`에 파일로 떨어뜨린다. 부모 컨텍스트에 원문이 쌓이면 이후 **모든 턴에서 다시 청구**된다.
- **서브에이전트 하나당 툴 호출 60회, 웹 검색 20건.** 200턴 돌며 컨텍스트를 300K까지 쌓은 조사 에이전트 하나가 수천만 토큰을 쓴다. `~/.claude/hooks/agent-budget.py`가 모든 Agent 프롬프트에 예산을 자동으로 덧붙이지만, 훅은 안전망이지 면허가 아니다 — 디스패치하는 쪽에서 먼저 범위를 좁혀라.
- **리서치 웨이브 팬아웃은 한 층, 동시 4개까지.** 조사 에이전트가 또 에이전트를 띄우지 않는다. 2026-09-16 프리플라이트에서 4개를 띄웠는데 중첩으로 9개가 병렬로 돌아 그날 토큰의 44%를 한 세션이 썼다.
- **전제가 바뀌어도 프리플라이트를 통째로 다시 돌리지 않는다.** 바뀐 축(객단가·규제 등)만 다시 조사하고, 나머지 갈래는 이미 나온 결론을 재사용한다. 죽은 아이디어의 조사 결과는 다음 아이디어의 프리플라이트 자료다.
- **검색은 한 번에 4건 이하로 묶는다.** 병렬로 8건씩 던지면 한 턴에 들어오는 양이 그만큼 커진다. 검색 한도는 프로세스 단위라 서브에이전트도 부모와 합산된다 — 병렬화로는 한도가 안 늘어난다.
- **단계가 끝나면 `/clear`한다.** 산출물이 `ideas/<slug>/`에 있으므로 손실이 없다. 재개는 `PIPELINE.md` → `PROGRESS.md` 순으로 읽어서 한다.
- **모델을 단계에 맞춘다.** 조사·수집은 sonnet, 판정·문서 작성은 opus. 게이트 판정처럼 틀리면 비싼 곳에만 상위 모델을 쓴다.
- **아티팩트·긴 HTML은 스크래치패드에 쓰고 게시한다.** 프로젝트 트리에 두면 이후 diff 알림으로 본문이 다시 들어온다.
- **소진이 이상하면 `/token-audit`.** 세션별 집계에서 `턴`과 `최대ctx` 열이 범인을 가리킨다. 검색 건수는 후행 지표다.

## 세션 운영

- **시작 시:** `ideas/PIPELINE.md`를 읽고, 작업 중인 `<slug>`의 `PROGRESS.md`를 읽는다. 마지막 미완료 단계에서 이어간다. "이전 세션에서 [단계]까지 완료. [다음 단계]부터 진행합니다"라고 알린다.
- **단계 완료 시:** `PROGRESS.md`와 `PIPELINE.md`를 갱신한다. 게이트 판정과 근거를 한 줄로 남기고, RAU 천장·유지 배수·운영 부하 열을 최신값으로 고친다.
- **종료·보류 시:** `PIPELINE.md`에 사유를 적는다. 디렉터리는 지우지 않는다. 죽은 아이디어의 원가 계산과 리서치는 다음 아이디어의 프리플라이트 자료다.
- **커밋:** 게이트를 하나 넘을 때마다 `ideas/<slug>/`를 커밋한다. 메시지는 `<slug>: G2 통과 — RAU 천장 240만, CPU 월 18원, 유지비 월 4,300만`처럼 판정과 숫자를 담는다.

## PIPELINE.md 형식

```markdown
| slug | 한 줄 문제 정의 | 현재 단계 | 마지막 게이트 | 판정 | RAU 천장 | CPU_월 | 유지 배수 | 운영 부하(주/100만) | 다음 액션 | 갱신일 |
|---|---|---|---|---|---|---|---|---|---|---|
| ai-care-robot | 독거노인 낙상 감지 | S3 | G2 | 통과 (조건부) | 140만 | 62원 (황색) | 0.4 (경로 1개) | 28h (자동화 필요) | POC 8주차 | 2026-09-16 |
```

숫자 칸은 추정값이면 `~`를 앞에 붙이고, POC 실측값이면 그대로 쓴다.

## 하지 말 것

- **수익성이 없다는 이유로 아이디어를 죽이는 것.** 이 하네스의 합격 기준은 이익이 아니라 유지 배수 1.0이다
- 도달 규모를 사람 수 대신 금액 TAM으로 적는 것
- CPU_월 계산식 없이 G2를 통과시키는 것
- 사람이 붙어야 돌아가는 구조를 "초기엔 수동으로"라고 넘기는 것. 100만 시점 자동화 경로가 문서에 없으면 탈락이다
- POC 계측값 없이 G3를 통과시키는 것. 데스크 리서치의 "고객 목소리"는 실사용 데이터의 대체가 아니다
- 팀 규모를 가정한 계획을 쓰는 것. 모든 공수는 1인 기준 주(週)로 적는다
- 스코어카드 없이 기획서를 쓰는 것
- 게이트 판정을 사용자가 원하는 쪽으로 기울이는 것
- 저장소 루트나 홈 디렉터리에 산출물을 만드는 것
